{% extends 'base.html.twig' %}

{% block title %}Hello InscriptionFormController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    {{ form_start(form) }}
        {{ form_label(form.name_user) }}
        {{ form_widget(form.name_user) }}    
        <br>    
        {{ form_label(form.first_name_user) }}
        {{ form_widget(form.first_name_user) }}
        <br>
        {{ form_label(form.mail_user) }}
        {{ form_widget(form.mail_user) }}
        <br>        
        {{ form_label(form.password_user) }}
        {{ form_widget(form.password_user) }}
        {{ form_rest(form) }}
        <br>
        <button type="submit">S'inscrire</button>
    {{ form_end(form) }}
{% endblock %}
